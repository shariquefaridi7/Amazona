

export const Reducers = (state = [], action) => {
    switch (action.type) {
        case "SETPRODUCTS":
            return (
                action.payload);

        default:
            return state


    }

}
